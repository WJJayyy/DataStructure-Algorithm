function solution(num1, num2) {
  var answer = Math.floor(num1 / num2);
  return answer;
}

function solution(num1, num2) {
  let result;
  if (num1 === num2) {
    result = 1;
  } else {
    result = -1;
  }
  return result;
}
