//1.
function solution(num1, num2) {
  let result;
  if (num1 === num2) {
    result = 1;
  } else {
    result = -1;
  }
  return result;
}

//2.
function solution(num1, num2) {
  return num1 === num2 ? 1 : -1;
}
